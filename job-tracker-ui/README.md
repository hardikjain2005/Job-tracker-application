# Job Tracker UI

React + TypeScript frontend (Vite) for Job Tracker. Setup, configuration and the full project overview are in the [root README](../README.md).

```bash
npm install
npm run dev     # http://localhost:5173
```
